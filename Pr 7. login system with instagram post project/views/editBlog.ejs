<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blog</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f8f9fa;
        }

        .card {
            max-width: 500px;
            margin: auto;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background: white;
        }

        .btn-primary {
            background: #007bff;
            border: none;
            padding: 10px;
            width: 100%;
            border-radius: 5px;
            font-size: 16px;
        }

        .btn-primary:hover {
            background: #0056b3;
        }

        .btn-secondary {
            text-decoration: none;
            margin-top: 15px;
        }

        .image-preview {
            display: block;
            margin-top: 10px;
            border-radius: 5px;
        }
    </style>

</head>

<body>

    <div class="container mt-5">
        <h2 class="text-center mb-4">Edit Your Blog</h2>

        <div class="card">
            <form action="/updateblogdata" method="post" enctype="multipart/form-data">
                <input type="hidden" name="editId" value="<%= oneRow?.id %>">

                <div class="mb-3">
                    <label class="form-label">Title:</label>
                    <input type="text" name="title" class="form-control" value="<%= oneRow?.title %>" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Description:</label>
                    <textarea name="description" class="form-control" rows="4" required><%= oneRow?.description %></textarea>
                </div>

                <div class="mb-3">
                    <label class="form-label">Upload New Image:</label>
                    <input type="file" name="image" class="form-control" accept="image/*">
                    <img src="<%= oneRow?.image %>" width="100" class="image-preview" alt="Current Image">
                </div>

                <button type="submit" class="btn btn-primary">Update Blog</button>
            </form>
        </div>

        <div class="text-center my-3">
            <a href="/viewblogpage" class="btn btn-secondary">Back to Blogs</a>
        </div>
    </div>

</body>

</html>
