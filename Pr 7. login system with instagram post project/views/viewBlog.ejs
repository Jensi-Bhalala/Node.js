<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogging</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }

        .header {
            background: #007bff;
            padding: 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .header .nav a {
            color: white;
            text-decoration: none;
            margin: 0 15px;
            font-weight: bold;
        }

        .button {
            background: white;
            color: #007bff;
            padding: 8px 15px;
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
        }

        .blog-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
        }

        .blog-card {
            width: 350px;
            background: white;
            padding: 15px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .blog-card img {
            width: 100%;
            border-radius: 10px;
            height: 200px;
            object-fit: cover;
        }

        .blog-title {
            font-size: 18px;
            font-weight: bold;
            margin-top: 10px;
        }

        .blog-description {
            font-size: 14px;
            color: #555;
            margin-top: 5px;
        }

        .blog-actions {
            margin-top: 10px;
        }

        .delete-btn, .edit-btn {
            text-decoration: none;
            padding: 5px 10px;
            border-radius: 5px;
            margin: 5px;
        }

        .delete-btn {
            background: red;
            color: white;
        }

        .edit-btn {
            background: green;
            color: white;
        }

        .add-blog-btn {
            display: block;
            width: 200px;
            margin: 20px auto;
            text-align: center;
            background: #007bff;
            color: white;
            padding: 10px;
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
        }
    </style>
</head>

<body>

    <header class="header">
        <nav class="nav">
            <a href="/dashboard">Home</a>
            <a href="/viewblogpage">Your Blogs</a>
            <a href="/addblogpage">Add Blog</a>
        </nav>
        <a href="/logoutuser" class="button">Logout</a>
    </header>

    <div class="container mt-5">
        <h2 class="text-center mb-4">Your Blogs</h2>

        <div class="blog-container">
            <% allBlogs.map((val)=> { %>
                <div class="blog-card">
                    <img src="<%= val?.image %>" alt="Blog Image">
                    <div class="blog-title"><%= val?.title %></div>
                    <div class="blog-description"><%= val.description %></div>
                    <div class="blog-actions">
                        <a href="/deletedata?delId=<%= val.id %>" class="delete-btn">Delete</a>
                        <a href="/editblogdata?editId=<%= val.id %>" class="edit-btn">Edit</a>
                    </div>
                </div>
            <% }) %>
        </div>

        <a href="/addblogpage" class="add-blog-btn">Add Blog</a>
    </div>

</body>

</html>
