<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
</head>

<style>
    body {
        font-family: Arial, sans-serif;
        background-color: #f0f2f5;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
    }

    div {
        background-color: #e9bfdf;
        padding: 3%;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        width: 460px;
        text-align: center;
    }

    h2 {
        margin-bottom: 20px;
        color: #c404c4;
        font-size: 35px;
    }

    table {
        margin: 0 auto;
        border-spacing: 0;
        width: 100%;
    }

    td {
        padding: 10px;
        font-size: 19px;

    }

    input[type="text"] {
        width: 100%;
        padding: 8px;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 17px;
    }

    input[type="date"] {
        width: 100%;
        padding: 8px;
        border: 1px solid #ccc;
        color: #6c6b6b;
        border-radius: 5px;
        font-size: 17px;
    }

    input[type="submit"] {
        padding: 10px 20px;
        background-color: #911e74;
        margin-right: 80px;
        color: #fff;
        border: none;
        border-radius: 5px;
        font-size: 16px;

    }

    input[type="submit"]:hover {
        background-color: #9e2d95;
    }

    a {
        display: block;
        margin-top: 15px;
        color: #9e2d86;
        text-decoration: none;
        font-size: 17px;
    }

    a:hover {
        text-decoration: underline;
    }
</style>

<body>
    <div align="center">
        <h2>Edit Task</h2>
        <form method="post" action="updateuser">
            <input type="hidden" name="editid" value="<%= single.id %>">
            <table>
                <tr>
                    <td>Task</td>
                    <td><input type="text" name="usertask" value="<%= single.task %>"></td>
                </tr>
                <tr>
                    <td>Status</td>
                    <td><input type="text" name="userstatus" value="<%= single.status %>"></td>
                </tr>
                <tr>
                    <td> Dedline</td>
                    <td><input type="date" name="userdate" value="<%= single.deadline %>"></td>
                </tr>
                <tr>
                    <td></td>
                    <td> <input type="submit"></td>
                </tr>
            </table>
            <a href="/">View</a>
        </form>
    </div>
</body>

</html>