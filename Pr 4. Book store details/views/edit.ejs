<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Book</title>
   
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url("a.jpg");
            background-size: cover;
            background-position: center;
            display: flex;
            height: 100vh;
            justify-content: center;
            align-items: center;
            font-family: 'Roboto', sans-serif;
            color: #fff;
        }

        .container {
            background-color: rgba(0, 0, 0, 0.531);
            padding: 40px;
            margin: 2% 0 2% 0;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
        }

        h1 {
            font-size: 2rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
        }

        .form-control, .form-select {
            border-radius: 8px;
            margin-top: 5px;
        }

        .btn-secondary {
            background-color: #6c757d;
            border: none;
            padding: 10px 20px;
            border-radius: 8px;
            font-size: 1rem;
        }

        .btn-link {
            text-decoration: none;
            color: #ffffff;
            font-size: 1rem;
            margin-top: 10px;
        }

        .btn-link:hover {
            text-decoration: underline;
        }

        .form-label {
            font-weight: 500;
        }

        .d-grid {
            margin-top: 20px;
        }

        /* Mobile responsiveness */
        @media (max-width: 768px) {
            .container {
                padding: 20px;
            }

            h1 {
                font-size: 1.5rem;
            }
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-8 col-sm-12">
                <h1 class="text-center mb-4">Update Book Information</h1>

                <form method="post" action="/updateRecord">
                    <input type="hidden" name="editid" value="<%= data.id %>">

                    <div class="mb-3">
                        <label for="bookname" class="form-label">Book Name</label>
                        <input type="text" class="form-control" id="bookname" name="bookname" value="<%= data.bookname %>" required>
                    </div>

                    <div class="mb-3">
                        <label for="bookprice" class="form-label">Book Price</label> 
                        <input type="text" class="form-control" id="bookprice" name="bookprice" value="<%= data.bookprice %>" required>
                    </div>

                    <div class="mb-3">
                        <label for="bookpages" class="form-label">Book Pages</label>
                        <select class="form-select" id="bookpages" name="bookpages" required>
                            <option selected disabled>---Select Pages---</option>
                            <% 
                                let pageOptions = ["1 to 100", "101 to 200", "201 to 300", "301 to 400"]; 
                                pageOptions.forEach((ci) => { %>
                                    <option value="<%= ci %>" <%= data.bookpages === ci ? "selected" : "" %>><%= ci %></option>
                            <% }) %>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label for="bookauthor" class="form-label ">Book Author</label> 
                        <input type="text" class="form-control" id="bookauthor" name="bookauthor" value="<%= data.bookauthor %>" required>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-secondary">Update</button>
                    </div>
                </form>

                <div class="text-center mt-3">
                    <a href="/" class="btn btn-link">Go Back</a> 
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
